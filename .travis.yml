language: python
os: linux
dist: focal
jobs:
  include:
    - name: "Python 3.8 on Linux (Ubuntu 20.04 Focal Fossa)"
      python: 3.8
    - name: "Python 3.8 on macOS"
      os: osx
      osx_image: xcode12u
      language: shell
    - name: "Python 3.8 on Windows"
      os: windows
      language: shell
      before_install:
        - choco install python --version 3.8
      env: PATH=/c/Python38:/c/Python38/Scripts:$PATH
script: python3 create.py -ni -l EN -f FETTI -t "Lab work" -s "New Subject" -ti "Title with 4 words" -fn John -ln "Doe Jr." -g 69420F || python create.py -ni -l EN -f FETTI -t "Lab work" -s "New Subject" -ti "Title with 4 words" -fn John -ln "Doe Jr." -g 69420F
